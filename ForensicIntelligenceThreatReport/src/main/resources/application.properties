spring.application.name=ForensicIntelligenceThreatReport
server.port=${API_PORT:8080}
server.address=0.0.0.0

#Database
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/Forensic
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:super}

# HIBERNATE
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false


# ELASTICSEARCH
elasticsearch.host=${ES_HOST:localhost}
elasticsearch.port=${ES_PORT:9200}
elasticsearch.userName=${ES_USERNAME:}
elasticsearch.password=${ES_PASSWORD:}


# DOCUMENT STORAGE - MINIO
spring.minio.url=http://${MINIO_HOST:localhost}:9000
spring.minio.bucket=${MINIO_BUCKET:reportbucket}
spring.minio.access-key=${MINIO_ACCESS_KEY:JI4WRIKJ3Q3YT38M0F0J}
spring.minio.secret-key=${MINIO_SECRET_KEY:+XDVc5IdnIh4RC2FZ7OxP0VvN+OtmPapDA5Yhwz4}

# LOGGING
# Log file configuration
logging.file.name=logstash-ingest-data/application.log
#logging.level.com.example=DEBUG #(optional)
spring.cloud.compatibility-verifier.enabled=false


#FILE LIMIT

pring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB


#GEO CODE

app.locationiq.api-key=pk.9a9db931fa64c111b2f6b9d04d06796d
